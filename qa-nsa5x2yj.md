# Integrated Release QA Evidence

## Responsive verification — 29 July 2026

| Route        | 1280 × 720                                                                                                                                                       | 375 × 812                                                                                                                                                             |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/send`      | AI tone adjustment, editable draft, compliance guidance, live preview, platform selector, and primary send action render without overlap or horizontal clipping. | AI tone controls, draft fields, preview summary, platform selector, and primary action remain reachable in one-column flow without horizontal overflow.               |
| `/import`    | Import Clients header, four-step indicator, template action, and CSV drop zone render with clear hierarchy and no overlap.                                       | Step labels, template action, and CSV drop zone wrap cleanly and remain touch-accessible without horizontal overflow.                                                 |
| `/dashboard` | Summary metrics, Email Performance, and All Activity render without overlap; the Activity Trend card was still in its loading skeleton at capture time.          | Summary metrics, Email Performance, and All Activity remain readable in the mobile layout; the Activity Trend card was still in its loading skeleton at capture time. |

The first responsive pass confirms the concurrent feature routes remain structurally sound after the workflow remediation. The current screenshot pass captured the Activity Trend card during its loading skeleton. A separate browser attempt did not share the authenticated preview session and correctly fell back to the public landing page, so it was not treated as dashboard evidence.

The exact concurrent checkpoint’s existing QA record (`qa-rufxugfe.md`) already verifies the settled Activity Trend card at desktop and 375-pixel mobile widths, including visible 30/60/90-day filters and reachable CSV/PNG exports. The integration remediation changes only GitHub workflow automation, its regression test, the active ledger, this QA evidence, and two test-file EOF characters; no dashboard, export, import, email-composer, localization, or runtime source was changed after that settled-state review. The prior settled-state evidence therefore remains applicable, while the new captures independently verify that the integrated tree preserved responsive route structure.
