# Authentication and Account-Management Verification

- Clean-session desktop capture at 1440 × 1000 rendered the public `/login` route after the application-loading period.
- The page shows the GetPhame brand lockup, public navigation, Apple sign-in option, returning-user email field, and **Send Magic Link** action with readable contrast and no visible clipping.
- A clean-session phone capture at 390 × 844 shows the same login choices in a single-column layout, with the compact brand header, language control, mobile menu, full-width email field, and full-width magic-link action visible without horizontal overflow.
- The first immediate screenshot captured only the application loading state; the page was recaptured with a six-second virtual rendering budget before evaluation.
- Automated coverage separately verifies the landing-page and onboarding-page returning-user sign-in entries, active-origin payload, existing-account dashboard routing, administrator authorization, merge direction, and account-data transfer categories.
- Final release gates passed: **32 test files**, **171 passing tests**, **1 skipped test**, strict TypeScript validation, and the production build.
- Published authenticated `/admin/users` captures at 1440 × 1000 and 390 × 844 rendered all 11 accounts, searchable status metadata, administrator and Life badges, role/Life actions, and the new Combine/Delete controls without clipping or horizontal overflow.
- Pagination is intentionally not rendered for the current 11-account directory because the configured page size is 25. Executable regression coverage retains the multi-page behavior and navigation controls for directories that exceed 25 accounts.
- The current administrator row renders self-demotion and self-deletion as disabled; Life mutations are also disabled for administrator rows. Runtime authorization tests cover non-admin rejection and server-side mutation safeguards without changing production accounts.
- The active repository has one registered worktree and no nested GetPhame/Mac clone, duplicate `.git` directory, or four-file untracked icon set. The audit changed neither the active working tree nor any external Mac directory; any previously observed external clone remains outside this sandbox and was not touched.
- A real production Apple authorization returned to `/api/auth/apple/callback` with an authorization `code` but no browser-posted `id_token`, causing the prior handler to redirect with `apple_missing_token`. The repair now performs Apple's required server-to-server authorization-code exchange, verifies the returned identity token, and then creates the application session.
- If Apple's verified email matches an established GetPhame account, the Apple subject is stored as a login-identity alias for that account instead of creating another user. The primary login identity, role, plan, and owned business data remain on the established account.
- Apple callback regressions cover successful code exchange, verified token parsing, existing-email identity linking, safe authorization cancellation, and token-exchange failure without account mutation. Final gates after the repair passed: **33 test files**, **175 passing tests**, **1 skipped test**, strict TypeScript validation, and the production build.
