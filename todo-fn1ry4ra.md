# Get Phame: Admin Controls, Trial Banner, and Diagnostics Delivery

- [ ] Add durable, admin-managed per-tier maximum send-burst caps and enforce them in bulk send flows.
- [ ] Add a localized, dismissible trial-days dashboard banner with a one-click monthly checkout action.
- [ ] Add an administrator-only "Generate Snapshot Now" control for on-demand diagnostic CSV delivery with bounded, idempotent handling.
- [ ] Update and validate the reusable protected-release synchronization skill using Skill Creator.
- [ ] Apply additive schema migration, run focused/full validation, production build, responsive checks, checkpoint, GitHub synchronization, and deployment verification.
