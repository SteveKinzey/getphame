# Quiet-hours validation notes

- **Desktop settings:** The business-local quiet-hours card renders in the existing Settings hierarchy, with physical-address, local-timezone, quiet-period, support-exception, and queued-delivery controls visible in the normal scroll flow.
- **Mobile settings (375 px):** The card remains in the single-column mobile layout without observed horizontal clipping; inputs and controls are stacked for touch interaction.
- **Administrator support inbox:** The inbox route renders correctly. Its current dataset has no support tickets, so a live exception-approval control requires an administrator test ticket for end-to-end visual confirmation.

