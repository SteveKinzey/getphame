# GitHub CI publication status — 2026-07-20

The GitHub Actions quality-gate workflow was validated locally and the repository owner completed GitHub sudo verification for a browser-based publication. The direct CLI push was blocked only because the configured GitHub App token lacks permission to create or update workflow files. The workflow was successfully committed directly to `main` in [GitHub commit `1a9ff17`](https://github.com/SteveKinzey/getphame/commit/1a9ff17). Quality Gate run #1 for that commit was superseded and cancelled by the workflow’s `cancel-in-progress` concurrency setting after the reconciled `41fb90f` merge was pushed. Quality Gate run #2 for `41fb90f` completed with a failure after 1 minute 57 seconds. Its summary reports process exit code `134`, alongside a non-blocking warning that the configured GitHub Actions currently target deprecated Node.js 20 and are being forced to run on Node.js 24. A GitHub workspace task surfaced the underlying type-check OOM and proposed a draft-only, two-line workflow change: set `NODE_OPTIONS=--max-old-space-size=4096` on the `pnpm check` step.

That scoped heap setting has now been independently applied and enforced by the workflow contract test. With the same `NODE_OPTIONS` value, the local TypeScript gate passed; the final merged release validation also passed its production OSV audit, 69 test files, and production build. The next pushed commit must still be observed in GitHub Actions before the hosted CI repair is considered verified.

GitHub Actions then started Quality Gate run #13 for main commit `8f8ec33`. At the first post-push check, the run was in progress; the prior main-branch Quality Gate run #10 remained recorded as failed, while unrelated Copilot-branch attempts also failed. The final status of run #13 is the authoritative hosted-CI result for this repair.

Run #13 completed as failed after 1 minute 16 seconds. Its job summary reports one failed test file and one failed test out of 369 total tests; this confirms the scoped TypeScript heap setting moved the hosted failure past the earlier type-check OOM, but a test-level regression still requires log-level diagnosis.

The detailed hosted log identified `server/resend.system-email.integration.test.ts` as the remaining failure. The test treated absent optional Resend SMTP variables as an assertion failure, even though public GitHub Actions does not receive those private integration credentials. The test now runs live SMTP verification only when all credentials are supplied, and runs its missing-variable assertion only when `RUN_SYSTEM_EMAIL_INTEGRATION=true` explicitly requests that verification. A credential-free CI-equivalent build then passed the production OSV audit (656 unique npm package versions), 68 test files with one intentional skip, 377 passing tests with two intentional skips, and the production bundle. A new hosted run is still required after this repair is pushed.

Quality Gate run #14 began for main commit `eac09ca` after the credential-aware test repair was pushed. At the first status check, it remained in progress; its final result will determine whether the hosted workflow is now green.

Quality Gate run #14 completed successfully in 2 minutes 17 seconds on main commit `eac09ca`. This confirms the push-triggered workflow now completes its frozen install, TypeScript check with the scoped heap limit, credential-free test suite, and production build in GitHub Actions.
